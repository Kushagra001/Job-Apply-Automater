# ── LLM ──────────────────────────────────────────────────────────────────────
groq==0.9.0                  # Groq SDK (Llama 3.3 70B)
httpx<0.28.0                 # Pin httpx to fix Groq proxy error

# ── Browser automation ────────────────────────────────────────────────────────
playwright>=1.48.0,<2.0.0     # Chromium scraping + ATS form filling (supports Ubuntu 24.04+ & 22.04)

# ── Google Sheets ─────────────────────────────────────────────────────────────
gspread==6.1.2               # Sheets read/write via service account
google-auth==2.30.0          # OAuth2 credentials for gspread

# ── PDF rendering ─────────────────────────────────────────────────────────────
# render_pdf.py uses Playwright/Chromium (already installed below) for HTML→PDF.
# WeasyPrint was removed: it requires GTK/Pango system libs unavailable on Windows.
jinja2==3.1.4                # Resume HTML template rendering
pypdf>=5.0.0                 # PDF page count & ATS text parseability verification

# ── HTTP / scraping helpers ───────────────────────────────────────────────────
requests==2.32.3             # JSON API fetches (RemoteOK, Remotive)
tenacity==8.3.0              # Retry logic with exponential backoff

# ── Config / env ──────────────────────────────────────────────────────────────
python-dotenv==1.0.1         # Load .env for local development

# ── Dev / testing ─────────────────────────────────────────────────────────────
pytest==8.2.2
pytest-asyncio==0.23.7

playwright-stealth==2.0.3
